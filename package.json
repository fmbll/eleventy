{
  "name": "eleventy-sample",
  "version": "1.0.0",
  "main": ".eleventy.js",
  "scripts": {
    "install": "npm install @11ty/eleventy --save-dev",
    "start": "npx @11ty/eleventy --serve --port=8080",
    "clean": "rm -rf site",
    "deploy": "npx @11ty/eleventy --pathprefix=/",
    "deploy-subdomain": "npx @11ty/eleventy --pathprefix=/subdirectory"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "description": "Eleventy Sample",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
